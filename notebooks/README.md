# Notebooks

These notebooks are legacy controllers from the older finite-wire workflow.

For new calculations, use:

1. `scripts/bulk_validate.py`
2. `scripts/prepare_paper_periodic_wire.py`
3. `scripts/run_periodic_tensile.py`

The current supported path is the paper-style periodic nanowire workflow with `TFVW` and `al.gga.recpot`.
